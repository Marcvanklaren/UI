package com.example.gerwindekruijf.ui;

import android.content.Intent;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;

public class MyGroupes_Activity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_my_groupes_);
    }

    //Event, button click to new Group
    // list linksboven voor alle opties


    /*Button Addbtn = (Button) findViewById(R.id.AddGroup_btn);

    Addbtn.setOnClickListener(new OnClickListener()
    {
        public void onClick(View v)
        {
            startActivity(new Intent(MyGroupes_Activity.this, AddGroup_Act.class));
        }
    }
    */
}
